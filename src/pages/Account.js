import {
	Avatar,
	Typography,
	List,
	ListItem,
	ListItemIcon,
	ListItemText,
	TextareaAutosize,
	Box,
	Button
} from '@material-ui/core/';

const Account = () => {
	return (
		<Box p={2} flexGrow={1}>
			<Typography variant="h3">Account Settings</Typography>
		</Box>
	);
};

export default Account;
